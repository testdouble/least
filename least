#!/usr/bin/env ruby

def main
  system("clear")
  loop do
    break if STDIN.eof?

    line = STDIN.readline
    puts line
  end
rescue Interrupt
  puts "Exiting..."
end

if __FILE__ == $0
  main
end
